<div class="container mt-5">

    <h2 class="mb-4 fw-bold">🔥 Khóa học nổi bật trong tuần</h2>
    <div class="row row-cols-1 row-cols-md-4 g-4">
        {{#each outstandingCourses}}
        <div class="col">
            <div class="card shadow-sm h-100 border-0">
                <img src="{{this.thumbnail}}" class="card-img-top" alt="{{this.title}}"
                    style="height: 180px; object-fit: cover;">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title text-truncate">{{this.title}}</h5>
                    <p class="card-text text-muted small">{{this.category}}</p>
                    <div class="d-flex align-items-center mb-1">
                        <span class="fw-bold text-warning me-1">{{this.rating_avg}}</span>
                        {{{generateStars this.rating_avg}}}
                        <span class="text-muted ms-1 small">({{this.rating_count}})</span>
                    </div>
                    <div class="mt-auto">
                        <div>
                            {{#if this.sale_price}}
                            <span class="fw-bold text-primary fs-5">{{formatVnd this.sale_price}}</span>
                            <span class="text-muted text-decoration-line-through ms-1 small">{{formatVnd
                                this.price}}</span>
                            {{else}}
                            <span class="fw-bold text-primary fs-5">{{formatVnd this.price}}</span>
                            {{/if}}
                        </div>
                        <div class="d-flex align-items-center mt-2 gap-2">
                            {{#if_contains ../ownedCourseIds this.id}}
                            <a href="/student/my-courses" class="btn btn-outline-success btn-sm w-100 disabled">
                                <i class="bi bi-check-circle"></i> Đã sở hữu
                            </a>
                            {{else}}
                            <form action="/cart/add" method="POST" style="flex-grow: 1;">
                                <input type="hidden" name="course_id" value="{{this.id}}">
                                <button type="submit" class="btn btn-success btn-sm w-100">
                                    <i class="bi bi-cart-plus"></i> Thêm vào giỏ
                                </button>
                            </form>
                            {{/if_contains}}
                            <a href="/courses/{{this.id}}" class="btn btn-outline-primary btn-sm" title="Xem chi tiết">
                                <i class="bi bi-info-circle"></i>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>

    <hr class="my-5">

    <h2 class="mb-4 fw-bold">👀 Khóa học được xem nhiều nhất</h2>
    <div class="row row-cols-1 row-cols-md-4 g-4">
        {{#each mostViewedCourses}}
        <div class="col">
            <div class="card shadow-sm h-100 border-0">
                <img src="{{this.thumbnail}}" class="card-img-top" alt="{{this.title}}"
                    style="height: 180px; object-fit: cover;">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title text-truncate">{{this.title}}</h5>
                    <p class="card-text text-muted small">{{this.category}}</p>
                    <div class="d-flex align-items-center mb-1">
                        <span class="fw-bold text-warning me-1">{{this.rating_avg}}</span>
                        {{{generateStars this.rating_avg}}}
                        <span class="text-muted ms-1 small">({{this.rating_count}})</span>
                    </div>
                    <div class="mt-auto">
                        <div>
                            {{#if this.sale_price}}
                            <span class="fw-bold text-primary fs-5">{{formatVnd this.sale_price}}</span>
                            <span class="text-muted text-decoration-line-through ms-1 small">{{formatVnd
                                this.price}}</span>
                            {{else}}
                            <span class="fw-bold text-primary fs-5">{{formatVnd this.price}}</span>
                            {{/if}}
                        </div>
                        <div class="d-flex align-items-center mt-2 gap-2">
                            {{#if_contains ../ownedCourseIds this.id}}
                            <a href="/student/my-courses" class="btn btn-outline-success btn-sm w-100 disabled">
                                <i class="bi bi-check-circle"></i> Đã sở hữu
                            </a>
                            {{else}}
                            <form action="/cart/add" method="POST" style="flex-grow: 1;">
                                <input type="hidden" name="course_id" value="{{this.id}}">
                                <button type="submit" class="btn btn-success btn-sm w-100">
                                    <i class="bi bi-cart-plus"></i> Thêm vào giỏ
                                </button>
                            </form>
                            {{/if_contains}}
                            <a href="/courses/{{this.id}}" class="btn btn-outline-primary btn-sm" title="Xem chi tiết">
                                <i class="bi bi-info-circle"></i>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>

    <hr class="my-5">

    <h2 class="mb-4 fw-bold">✨ Khóa học mới nhất</h2>
    <div class="row row-cols-1 row-cols-md-4 g-4">
        {{#each newestCourses}}
        <div class="col">
            <div class="card shadow-sm h-100 border-0">
                <img src="{{this.thumbnail}}" class="card-img-top" alt="{{this.title}}"
                    style="height: 180px; object-fit: cover;">
                <div class="card-body d-flex flex-column">
                    <h5 class="card-title text-truncate">{{this.title}}</h5>
                    <p class="card-text text-muted small">{{this.category}}</p>
                    <div class="d-flex align-items-center mb-1">
                        <span class="fw-bold text-warning me-1">{{this.rating_avg}}</span>
                        {{{generateStars this.rating_avg}}}
                        <span class="text-muted ms-1 small">({{this.rating_count}})</span>
                    </div>
                    <div class="mt-auto">
                        <div>
                            {{#if this.sale_price}}
                            <span class="fw-bold text-primary fs-5">{{formatVnd this.sale_price}}</span>
                            <span class="text-muted text-decoration-line-through ms-1 small">{{formatVnd
                                this.price}}</span>
                            {{else}}
                            <span class="fw-bold text-primary fs-5">{{formatVnd this.price}}</span>
                            {{/if}}
                        </div>
                        <div class="d-flex align-items-center mt-2 gap-2">
                            {{#if_contains ../ownedCourseIds this.id}}
                            <a href="/student/my-courses" class="btn btn-outline-success btn-sm w-100 disabled">
                                <i class="bi bi-check-circle"></i> Đã sở hữu
                            </a>
                            {{else}}
                            <form action="/cart/add" method="POST" style="flex-grow: 1;">
                                <input type="hidden" name="course_id" value="{{this.id}}">
                                <button type="submit" class="btn btn-success btn-sm w-100">
                                    <i class="bi bi-cart-plus"></i> Thêm vào giỏ
                                </button>
                            </form>
                            {{/if_contains}}
                            <a href="/courses/{{this.id}}" class="btn btn-outline-primary btn-sm" title="Xem chi tiết">
                                <i class="bi bi-info-circle"></i>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{/each}}
    </div>


    <hr class="my-5">

    <h2 class="mb-4 fw-bold">🏆 Lĩnh vực được đăng ký nhiều nhất tuần</h2>
    <div class="row">
        <div class="col-md-6">
            <ul class="list-group list-group-flush">
                {{#each topCategories}}
                <li class="list-group-item d-flex justify-content-between align-items-center fs-5 p-3">
                    <a href="/categories/{{this.id}}" class="text-decoration-none text-dark fw-bold">
                        {{this.catname}}
                    </a>
                    <span class="badge bg-primary rounded-pill">{{this.enroll_count}} lượt đăng ký</span>
                </li>
                {{/each}}
            </ul>
        </div>
    </div>

</div>